{% extends 'base.html.twig' %}

{% block title %}Hello TopicController!{% endblock %}

{% block body %}
<div class="js-filter">
    <h2>{{ category.name }}</h2>
              
        {% for topic in category.topics %}
            <div class="topic">
                <a href="{{ path('app_post', {'topicId': topic.id}) }}">{{topic.title}}</a>
            </div>
        {% endfor %}  
</div>

{% endblock %}