{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
<div id="page">
    <div id="page-user">

        <div id="image-user">
            <img src="https://picsum.photos/150"></br>
        </div>

        <div id="username-user">
            <h1>{{app.user.username}}</h1>

        </div>

        <div id="email-user">
            <h6>{{app.user.email}}</h6>
        </div>

        <a href="{{ path('app_deckUser', {'id': user.id}) }}">Decks de {{app.user.username}}</a>
        <a href="{{ path('app_topic', {'id': user.id}) }}">Topic</a>
    </div>
</div>
{% endblock %}